// src/main.jsx - This should ONLY contain JavaScript/JSX
import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';
import './index.css';  // CSS should be imported, not written directly

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
